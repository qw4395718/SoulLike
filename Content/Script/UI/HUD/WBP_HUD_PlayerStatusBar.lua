--
-- DESCRIPTION
--
-- @COMPANY **
-- @AUTHOR **
-- @DATE ${date} ${time}
--

---@type WBP_HUD_PlayerStatusBar_C
local M = UnLua.Class()

--function M:Initialize(Initializer)
--end

--function M:PreConstruct(IsDesignTime)
--end

-- function M:Construct()
-- end

--function M:Tick(MyGeometry, InDeltaTime)
--end

function M:AddPlayerStatus(addStatusArr)
    self:AddPlayerStatus(addStatusArr)
    -- 添加视听效果
end

function M:UpdatePlayerStatus(updateStatusArr)
    self:UpdatePlayerStatus(updateStatusArr)
    -- 添加视听效果
end

function M:RemovePlayerStatus(removeStatusArr)
    self:RemovePlayerStatus(removeStatusArr)
    -- 添加视听效果
end

return M
